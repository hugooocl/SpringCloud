server:
  port: 20001
eureka:
  instance:
    hostname: server2
  client:
    #register-with-eureka: false
    #fetch-registry: false
    service-url:
      defaultZone: http://localhost:20002/eureka/

spring:
  application:
    name: eureka-cluster